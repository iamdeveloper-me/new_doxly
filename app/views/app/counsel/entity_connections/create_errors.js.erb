if($('.errors-container').length > 0){
  $('.errors-container').remove()
}
$('.modal-body').prepend("<%= escape_javascript render('app/shared/errors', errors: @entity.errors.full_messages) %>")
App.Modal.bindForms()
